{
  "name": "buildhq",
  "version": "0.7.2",
  "description": "AI DIY design-to-build platform — StructureGraph, Forge, Help Me Build",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "smoke": "npx tsx src/index.ts",
    "dev": "npx tsx watch src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.10.0"
  }
}
