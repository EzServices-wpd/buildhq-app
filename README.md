# BuildHq

AI DIY design-to-build platform (Closet + Forge + StructureGraph).

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Deploy

Connect this repo to Vercel → Framework: Next.js → Deploy.

## Version

0.7.3 — root config restored (Next.js app working again).
